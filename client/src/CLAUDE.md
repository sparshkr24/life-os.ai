# CLAUDE.md — `client/src/`

## Responsibility

All TypeScript application code for the Expo app. UI shell lives in
`client/App.tsx`; per-tab screens are in `screens/`. This folder owns:
local SQLite (`db/`), the native-bridge wrapper (`bridge/`), read-only repos
(`repos/`), secure-store helpers (`secure/`), and screen components (`screens/`).

Stage 4 lands schema v2 + observability tabs. Stage 5 lands the 15-min
aggregator pipeline (`aggregator/`). Stage 6 lands the rule engine + 3-level
local notifications (`rules/`). Brain and chat wiring arrive in later stages.

## Files

| File | Purpose |
|---|---|
| `db/schema.ts` | Phone SQLite DDL (`PHONE_SCHEMA_SQL`), `SCHEMA_VERSION = 4`, row types: `EventRow`, `DailyRollupRow` (now incl. `productivity_score`), `MonthlyRollupRow`, `BehaviorProfileRow`, `RuleRow`, `NudgeRow` (now incl. `next_day_score`/`baseline_score`/`score_delta`/`user_helpful`), `LlmCallRow`, `AppCategoryRow`, `PlaceRow`, **v4: `MemoryRow` + `MemoryType`**. `EventKind` includes v3 values `inferred_activity` and `user_clarification`. `LlmPurpose` v4 adds `'embed'`. `nudges_log.user_helpful` (1=▲, -1=▼, NULL=unrated) is the user's manual thumbs feedback, INDEPENDENT of the LLM-computed `score_delta`. v4 adds the `memories` table (id/created_ts/type/summary/cause/effect/impact_score/confidence/occurrences/reinforcement/contradiction/last_accessed/decay_factor/tags JSON/source_ref/rollup_date/embedding JSON/embed_model/predicted_outcome/actual_outcome/was_correct/archived_ts/parent_id/child_ids JSON) + 3 indexes. Created via `CREATE TABLE IF NOT EXISTS` so existing DBs pick it up automatically on next `migrate()`. |
| `db/seed.ts` | First-launch reference data: `SEED_APP_CATEGORIES`, `SEED_RULES`. |
| `db/index.ts` | DB lifecycle: `getDb`, `withDb`, `migrate`, `describeDb`, `reopenDb`, `purgeShortAppFg`, plus internal `addColumnIfMissing` for additive columns. Single open connection (`useNewConnection: true` to bypass the native zombie cache), WAL on. `withDb(fn)` is the canonical entry point for queries — auto-recovers from stale-SharedRef NPEs by reopening once. `migrate()` runs `purgeShortAppFg(db, 1000)` on every boot, then unconditionally backfills v2-retroactive `nudges_log` columns (`source/rule_id/llm_call_id/reasoning/level`) and v3 columns (`productivity_score`, `nudges_log.{next_day_score,baseline_score,score_delta,user_helpful}`). The unconditional run is critical — users whose DB was created before `nudges_log` got those columns will hit `no such column: level` when smart-nudge inserts; the migration heals them on next boot. |
| `ingest/cleanup.ts` | `cleanupRawEvents()` — the JS-side ingestion pipeline. Single source of truth for post-write event cleanup. Runs at the start of every aggregator tick (before rollups). Three rules today: (1) drop `app_fg` rows whose `pkg` is in a noise denylist (`android`, launchers, settings, systemui, etc.) catching what slipped past Kotlin's write-time filter; (2) merge same-pkg `app_fg` sessions within 90s of each other in the last 24h (covers Kotlin service restarts that reset the in-memory dedup); (3) `purgeShortAppFg(1000)`. Idempotent, bounded. Also exports `WAKE_NOISE_PKGS` (launchers + lock-screen + alarm clocks + dialers) consumed by `aggWakeFirstApp`. **Add new cleanup rules here, not in collectors.** |
| `aggregator/silence.ts` | `classifySilences(db, date, tz)` — finds gaps ≥60 min in the active-event stream and writes `events.kind='inferred_activity'` with `label` ∈ {sleep_or_rest, focused_work, workout, unknown}. Uses last `geo_enter`/`geo_exit` for place context. Idempotent per day; preserves rows with `user_confirmed: true`. Called from the Stage-5 aggregator tick for today + yesterday. |
| `aggregator/time.ts` | Shared time helpers: `localHour`, `localDateStr`, `localMonthStr`, `localDayStartMs` (±14h IANA-tz search window), `prevDate`, `nextDate`, `deviceTz`. Hermes `Intl.DateTimeFormat` based — no Luxon. |
| `aggregator/rollup.ts` | `rebuildDailyRollup(db, date, tz)` — single-responsibility daily-rollup orchestrator. UPSERTs `daily_rollup`. Owns the entire pipeline: `aggApps` (joins `app_categories`), `sumByCategory`, `aggByHour` (per-hour walk), `aggLateNight` (21:00–26:00 overlap), `aggSleep` (longest segment in [−12h, +14h]), `aggWakeFirstApp`, `aggPlaces` (geo_enter/exit walk + carry from prior day), `aggTransitions`, `aggSteps`, `aggActiveMinutes`, `aggTodos`, `aggNudges`, `aggSilences`. Exports `DailyRollupData`, `AppAgg`, `SleepAgg`, `prevDate`, `nextDate`. Exceeds 400 LOC by design — see hard rule #12. |
| `aggregator/monthlyFold.ts` | `foldMonth(db, month)` — rolls all `daily_rollup` rows for `'YYYY-MM'` into a `monthly_rollup` row (top apps, sleep p50/p90, place hours, totals, avg productivity score). Idempotent. |
| `aggregator/index.ts` | `runAggregatorTick()` — the public entry point. Per tick: `cleanupRawEvents()` (ingest pipeline) → `classifySilences(today,yest)` → `rebuildDailyRollup(today,yest)` → `computeProductivityScore(today,yest)` → once-per-day `foldMonth(prevMonth)` (gated via `schema_meta.last_monthly_fold_date`) → `runRulesOnceFromBackground()` → `runSmartNudgeTick()` (Stage 7, gated; never throws) → `maybeRunNightlyWatchdog()` (Stage 8, gated by 03:00 local + 20 h cooldown). Records `last_aggregator_ts`. Returns `TickReport`. |
| `rules/notify.ts` | `expo-notifications` wrapper. Three Android channels (`lifeos.silent`/`headsup`/`modal`) match levels 1/2/3 (LOW/DEFAULT/MAX importance, escalating vibration). `ensureNotificationChannels()` is idempotent; `fireNudgeNotification({level,title,body,data})` requests POST_NOTIFICATIONS once and schedules a 1s-delayed local notif on the right channel. |
| `rules/engine.ts` | `evaluateRules({tz?})` — pure rule evaluator. Loads enabled `rules`, parses `trigger`/`action` JSON, gates each rule by `cooldown_min` (no `nudges_log` row in window), matches one of 3 trigger shapes (see file header), and on hit fires a notification + INSERTs a `nudges_log` row (`source='rule'`). Helpers compute today's app/category minutes (incl. open sessions), today's wake_ts (longest sleep segment), and current place label. |
| `rules/worker.ts` | Worker plumbing. `startRulesForegroundLoop()` is called from App.tsx — 60 s `setInterval` + AppState `'active'` re-fire. `runRulesOnceFromBackground()` is called from the 15-min aggregator tick to cover app-killed windows. `lastRulesTickTs()` powers the Today screen status row. |
| `aggregator/worker.ts` | Background-worker plumbing. `TaskManager.defineTask(AGGREGATOR_TASK)` runs at module import; `registerAggregatorTask()` registers a 15-min `expo-background-fetch` periodic task (Android → WorkManager). `aggregatorTaskStatus()` powers the Today screen status row. |
| `brain/productivityScore.ts` | `computeProductivityScore(db, date)` — pure SQL UPDATE. 5-component weighted score in [0,1] (sleep 30, focus 25, wake 15, move 15, nudge 15). No LLM. Called by aggregator after every `daily_rollup` rebuild. |
| `brain/nudgeEffectiveness.ts` | `computeNudgeEffectiveness(db, forDate)` — nightly job step. For each nudge with a `user_action` on `forDate`, fills `next_day_score` / `baseline_score` (7-day median) / `score_delta` on `nudges_log`. Pure SQL, no LLM. |
| `brain/verifiedFacts.ts` | `buildVerifiedFacts(db)` — produces the `VERIFIED_FACTS` block for the nightly Sonnet call. Currently emits `low_phone_night` correlation when ≥5 days exist on each side; null otherwise. Add new correlations as new fns and append to the array. |
| `brain/behaviorProfile.types.ts` | v3 type defs: `BehaviorProfileV3`, `CausalChain`, `DayAttribution`, `RuleSuggestion*`, `SilencePriors`, `SilenceCorrelation`. Used to validate Sonnet output before persistence. |
| `brain/nightly.prompt.ts` | `NIGHTLY_SYSTEM_PROMPT` constant + `buildNightlyUserPrompt({prior, days, months, verifiedFacts})`. The Stage-8 runner imports these — no other file holds nightly prompt text. |
| `brain/smartNudge.ts` | Stage 7. `runSmartNudgeTick()` — every-15-min gpt-4o-mini smart-nudge layer. Pipeline inside one file: cost-cap check (`sumTodayLlmCostUsd` against `loadSnapshot().dailyCapUsd`) → key check → 90 min smart-cooldown via `nudges_log` → context build (today rollup + last 24 h nudges + current place) → `fetch` POST to `https://api.openai.com/v1/chat/completions` (no SDK) with `response_format: { type: 'json_object' }` → strict-JSON parse → always log `llm_calls` (purpose='tick', model='gpt-4o-mini') → on `should_nudge=true`, fire notification + write `nudges_log` (`source='smart'`, `llm_call_id` set). Pricing constants `PRICE_INPUT_PER_M = 0.15` and `PRICE_OUTPUT_PER_M = 0.60` USD per 1M tokens — update in one place when pricing shifts. Exports `runSmartNudgeTick`, `sumTodayLlmCostUsd`. |
| `brain/nightly.ts` | Stage 8. `runNightlyRebuild()` + `maybeRunNightlyWatchdog()` + `lastNightlyTs()`. Pipeline inside one file: cost-cap check (`sumTodayLlmCostUsd` vs `dailyCapUsd`) → anthropic key check → finalise yesterday's `productivity_score` → `computeNudgeEffectiveness` for last 7 days → load PRIOR profile + last 30 `daily_rollup` rows + last 3 `monthly_rollup` rows + `buildVerifiedFacts` → `fetch` POST to `https://api.anthropic.com/v1/messages` (no SDK) with `claude-sonnet-4-5-20250929`, `system=NIGHTLY_SYSTEM_PROMPT`, user prompt from `buildNightlyUserPrompt` → strip ```json fences → validate v3 sentinel keys (`causal_chains`, `rule_suggestions`) → always log `llm_calls` (purpose=`'nightly'`) → on valid output, UPSERT `behavior_profile` (id=1) and write `schema_meta.last_nightly_ts`. Watchdog gate: only fires when local hour ≥ 3 AND >20h since last success. Pricing constants `PRICE_INPUT_PER_M = 3.0` / `PRICE_OUTPUT_PER_M = 15.0` USD per 1M tokens. |
| `bridge/lifeOsBridge.ts` | Typed wrapper around `NativeModules.LifeOsBridge` (Kotlin). Methods: `startService`, `hasUsageAccess`, `openUsageAccessSettings`, `getStats`, plus Stage 3b/3c/3d permission helpers (`hasActivityRecognitionPermission`, `requestActivityRecognitionPermission`, `hasLocationPermissions`, `requestForegroundLocation`, `requestBackgroundLocation`, `setGeofences`, `removeAllGeofences`, `hasNotificationListenerAccess`, `openNotificationListenerSettings`, `isHealthConnectAvailable`, `openHealthConnect`). Exports `LifeOsPlace`. |
| `secure/keys.ts` | `expo-secure-store` helpers: `loadSnapshot`, `setAnthropicKey`, `setOpenAiKey`, `setDailyCap`, `getAnthropicKey`, `getOpenAiKey`. Default cap = $0.30/day. Snapshot only exposes the last 4 chars of stored keys. |
| `repos/observability.ts` | Read-only queries powering tabs: `listEvents` (default limit 1000), `eventCounts`, `eventTotalCount`, `listDailyRollups`, `listMonthlyRollups`, `listLlmCalls`, `todayLlmSpendUsd`, `listNudges`, `getProfile`, `setNudgeUserHelpful(id, value)` (writes `nudges_log.user_helpful`), `getLatestDailyRollup()`, `recentProductivityScores(days)` (returns oldest→newest for sparklines). All routed through `withDb` for stale-ref safety. |
| `theme.tsx` | 3 themes (`dark` / `light` / `modern`) with extended `ThemeTokens` (primary/secondary/tertiary `accent`/`accent2`/`accent3`, per-event-kind `kindColors`, segmented-strip tokens `segBg`/`segActiveBg`/`segText`/`segActiveText`, `glass*`/`headerGrad*`). `monoFont = 'JetBrainsMono_400Regular'` (loaded in `App.tsx` via `@expo-google-fonts/jetbrains-mono`). Persisted via `expo-secure-store` under `UI_THEME`. |
| `toast.tsx` | `ToastProvider` + `useToast()`. Tiny non-blocking toast surface (`info`/`error`/`ok`). Wrapped around `<Shell>` in `App.tsx`. Used by every async repo call to surface failures so the UI never appears silently broken. |
| `screens/index.ts` | Barrel re-exporting `TodayScreen`, `ObservabilityScreen`, `ChatScreen`, `ProfileScreen`, `SettingsScreen`, `TabId`. |
| `screens/shared.tsx` | Helpers + styles + `ActionButton` shared by every screen: `fmtTime/fmtTimeShort/fmtClock`, `fmtDur`, `parseEvent`, `prettyPkg`, `safeJson`, `truncate`, `useAsyncRunner` (try/catch + toast + loading), `makeStyles(theme)` factory, `NAV_CLEAR`, type `TabId = 'today' \| 'observe' \| 'chat' \| 'profile' \| 'settings'`. `'profile'` is reachable only from Settings ("View profile →"), not from the bottom nav. |
| `screens/widgets.tsx` | Reusable UI atoms used across the redesigned screens: `AppIcon` (real brand glyph via `@expo/vector-icons` FontAwesome5 — snapchat / instagram / youtube / whatsapp / slack / spotify / chrome / discord / reddit / telegram / github / linkedin / facebook / twitch / pinterest / google. Falls back to first-letter on a color-tinted square when no FA5 brand exists), `ScoreBar` (0-100 horizontal bar with optional baseline tick), `Sparkline` (bar-based 7-pt mini chart, no SVG dep), `SectionHeader`, `StatusDot` (with optional glow shadow), `PressableScale` (iOS-style 0.96-scale press built on the built-in `Animated` API), `kindTint(theme, kind)`. |
| `screens/Today.tsx` | Redesigned hero screen. Top: date + tracking pill (`StatusDot` color = service health: green<5min, amber<30min, red otherwise). Hero card: huge productivity score (56px JetBrainsMono), ▲/▼ delta vs 7-day baseline, `ScoreBar`, `Sparkline` of last 7 days. Then sleep card (bedtime→wake), top-3 apps card with brand `AppIcon`s + category-tinted labels, today's nudges (≤3) using `NudgeRowToday` subcomponent with `PressableScale` thumbs ▲/▼ that toggle `setNudgeUserHelpful`. Bottom: collapsible System card with `SystemRow` helpers wrapping all `Run aggregator/rules/smart/nightly now` buttons + collector-restart fallback. |
| `screens/Observability.tsx` | `ObservabilityScreen` — 4-tab segmented strip (Events / Rollups / LLM / Nudges). |
| `screens/EventsTable.tsx` | Infinite-scroll FlatList. Each row shows a brand `AppIcon` avatar (only the icon is colored, not the row); preserves the slim left-edge `eventTint` as a secondary kind cue. Uses `offsetRef` + Set-based dedup; calls `reopenDb()` on initial load to dodge stale WAL snapshots from the Kotlin writer; defers initial fetch via `InteractionManager.runAfterInteractions`. |
| `screens/RollupsScreen.tsx` | Daily/Monthly rollup **dashboard**. List shows compact tiles (date + score + top-app brand icon). Tap opens `RollupDetailSheet` — a Modal-based bottom sheet anchored at 50% screen height with eased slide-up (cubic-bezier `0.22,1,0.36,1`), backdrop fade, drag-handle to dismiss (`PanResponder`, 80px threshold). Inside: hero score with `▲`/`▼` delta vs previous day, Morning row (woke up, sleep + delta, first pickup), screen/steps/nudges row (with deltas where applicable), Top apps using **`a.minutes`** (was previously misread as `a.total_ms` causing NaN), Time-split stacked bar with legend, Insights card (heuristic productivity projection + yesterday's productive anchor). Monthly view uses `top_apps[].total_minutes`. Field names mirror `aggregator/rollup.ts` `AppAgg` and `monthlyFold.ts` `AppMonth` exactly. |
| `screens/LlmTable.tsx` | LLM-calls log filtered by purpose; shows today's spend; per-row request/response/error expander. |
| `screens/NudgesTable.tsx` | Nudges feed (replaces the old table). Filter chips (All / Rule / ✦ Smart / Helpful / Annoying), grouped by Today / Yesterday / Earlier. Each card: level-tinted `StatusDot`, bold message, `✦ smart` badge if smart-sourced, meta line with `LLM: helped/hurt/neutral` derived from `score_delta`, and `PressableScale` thumbs ▲/▼ that call `setNudgeUserHelpful`. Tap card to expand reasoning. The user thumbs (`user_helpful`) is independent of the LLM's automated `score_delta` analysis — both feed nightly. |
| `screens/Chat.tsx` | Stage 9 chat. `KeyboardAvoidingView` + bubble list + ActivityIndicator while sending. Calls `runChatTurn(history)` from `brain/chat.ts`; surfaces `skipped` (`cost_cap`/`no_key`) as toasts + assistant messages; renders today's LLM spend + last reply cost in a footer line above the input toolbar. |
| `brain/chat.ts` | Stage 9. `runChatTurn(history)` runs a Sonnet tool-calling loop against local SQLite. Cost-cap + key gates first (hard wall). POSTs `/v1/messages` with `tools=[get_today_summary, get_daily_rollup, get_recent_rollups, get_profile, get_recent_nudges]`. On `stop_reason: tool_use`, runs the matching handler (read-only DB query, all narrowed to rollup-level data — raw events stay off-LLM per CLAUDE.md §7), appends `tool_result`, re-POSTs (max `TOOL_LOOPS=4`). Accumulates token totals, logs to `llm_calls` (purpose='chat'). Pricing constants `PRICE_INPUT_PER_M=3.0`, `PRICE_OUTPUT_PER_M=15.0` USD/1M. **Stage 13:** `buildChatSystemPrompt(history)` calls `retrieveContext({decisionType:'chat', queryText:lastUserText, k:6})` and appends `contextBlock` to `SYSTEM_PROMPT`. Empty store / embed failure → bare `SYSTEM_PROMPT` (v2 path). |
| `memory/embed.ts` | **Stage 12.** OpenAI `text-embedding-3-small` wrapper. `embedText(text)` — cost-cap (`sumTodayLlmCostUsd`) + key check first; POSTs `https://api.openai.com/v1/embeddings`; logs every call to `llm_calls` (purpose='embed'). Returns `{vector,model,inTokens,costUsd}` or `null` on cost-cap/no-key/HTTP error. Exports `EMBED_DIM=1536`, `EMBED_MODEL='text-embedding-3-small'`, and `cosineSim(a,b)`. Pricing `$0.02/1M` input tokens. |
| `memory/store.ts` | **Stage 12.** CRUD + scoring for the `memories` table. `createMemory(MemoryInput)` builds the embed input from summary+cause+effect+tags, calls `embedText`, then INSERTs (returns id) or returns null on embed failure — never persists a row without an embedding. `listActiveMemories`, `getMemoryById`, `touchMemories(ids)` (bumps `last_accessed`), `reinforceMemory`, `contradictMemory`, `archiveMemory` (soft-delete, sets `archived_ts`), `recordPredictionOutcome(id, actual, wasCorrect)`, `getMemoryStats()`. `computeEffectiveScore(Memory)` = raw impact + reinforcement·0.3·impact − contradiction·0.5·sign(impact), with exponential recency decay after 7 days idle. Hydration parses JSON `embedding`/`tags`/`child_ids`; bad-shape rows are skipped. Inline `uuidv4()` (no native crypto dep). |
| `memory/rag.ts` | **Stage 12.** `retrieveContext(RagQuery) → RagResult`. Embeds `queryText`, `listActiveMemories`, computes `cosineSim` against every active embedding, re-ranks by `0.5·sim + 0.2·exp(-daysOld/30) + 0.15·|impact| + 0.15·confidence` (+ small tag-overlap bonus), `touchMemories` on the top-k. Per-decision-type k defaults: nightly=12, rule_generation=18, chat=6, smart_nudge=4, prediction_update=6. Returns `{embedded:false, memories:[]}` on embed failure so callers can fall back to pre-RAG paths. Builds a markdown `contextBlock` ready to drop into a prompt. **Wired into `brain/nightly.ts` (Stage 13) and `brain/chat.ts` (Stage 13).** |
| `memory/extract.ts` | **Stage 13.** `runDailyMemoryExtraction(forDate)` — once-per-day rollup → memory candidates. Cost-cap + OpenAI key check first; gated by `schema_meta.last_extract_date` (skips if already done). Loads `daily_rollup` for `forDate` + 7-day baseline, calls `gpt-4o-mini` (`response_format: json_object`, temperature 0.2), validates each candidate (clamps impact/confidence, requires cause+effect for `causal`, requires `predicted_outcome` for `prediction`, drops |impact|<0.15), then `createMemory` per row. Logs `llm_calls` (purpose='extract'). Pricing constants `PRICE_INPUT_PER_M=0.15`, `PRICE_OUTPUT_PER_M=0.60`. Called from `brain/nightly.ts` for yesterday before consolidation. |
| `screens/Profile.tsx` | Standalone screen reachable from `Settings` (no longer in bottom nav). Accepts `onBack?: () => void` for the "‹ Settings" affordance. Reads `behavior_profile.data` (BehaviorProfileV3) and renders: hero (identity blurb + confidence pill + `built_ts` / `based_on_days` / model), observed habits (good/bad/time-wasters as bullet rows), verified anchors (silence_correlations with delta_next_day_score_pct), causal chains (A → B → C as colored pills via `CausalChainCard` + `Pill`), suggested rules (rule_suggestions with level badge + ghost "Add as rule" button — wiring deferred to Stage 11). All v3 keys are `unknown` so safe-parsed via `asString` / `asStringArray`. |
| `screens/Settings.tsx` | Sectioned-card layout matching the design mock. Sections: PROFILE (single-row entry that calls `onOpenProfile`, the only nav into `Profile.tsx`), THEME, TRACKING PERMISSIONS (`<PermissionsCard />`), API KEYS (Anthropic + OpenAI), COST & LIMITS (today's spend vs daily cap with a colored fill bar that goes amber→red as the cap fills), SYSTEM DEBUG (collector status / last profile rebuild). Receives `onOpenProfile: () => void`. |
| `screens/PermissionsCard.tsx` | Stage 3b/3c/3d native-permissions surface inside Settings. Six rows (usage / activity recognition / fg loc / bg loc / notif listener / Health Connect) with status dots + one-tap grant or open-settings actions; re-polls 500 ms after every action and on mount. |

## Schema bump policy

When `SCHEMA_VERSION` changes:
1. Bump in `db/schema.ts`.
2. Update affected row types in same file.
3. `grep` Kotlin for any column you renamed/dropped (`client/android/app/src/main/java/com/lifeos`).
4. On phone: `adb uninstall com.lifeos` then reinstall — wipes the v(N-1) DB.

## LLM cost cap

Hard wall: $0.30/day default. Every LLM call (Stages 7–9) **must**:
1. `SELECT SUM(cost_usd) FROM llm_calls WHERE ts >= start_of_day` first.
2. Compare against `loadSnapshot().dailyCapUsd`.
3. Refuse and write a row with `ok=0, error='cap_exceeded'` if over.

## Call flow — app boot (Stage 4)

```
App.tsx mount
  → migrate()                          // db/index.ts
      → getDb() (lifeos.db, WAL)
      → exec all PHONE_SCHEMA_SQL stmts in tx
      → if schema_meta.version < 2: seedReference() + bump
  → if android: LifeOsBridge.hasUsageAccess() / startService()
  → render TabShell

User taps tab
  → App.tsx bottom-nav switches `tab` state
  → screens/index.tsx <Tab>Screen
      → repos/observability.ts query
      → render rows (tables in Observability)
```

## Top-level tabs (locked)

Bottom nav: **Today / Observe / Chat / Settings**. No other top-level tabs.
Deeper views live inside one of those four (e.g. Profile is a section inside Settings; raw event/rollup/llm/nudge tables are sub-sections of Observe).

## Hard rules (inherited from root CLAUDE.md)

- No `any`. No tutorial comments. No abstractions for one call site.
- Local-first: never reintroduce a server.
- Raw events never go to an LLM. Only `behavior_profile` + rollups.
- Schema is JS-owned. Kotlin only INSERTs against columns defined here.
