Drop your signed APK here as `lifeos.apk` so the Download buttons work.

From the main project, copy the release APK:

    cp ../client/android/app/build/outputs/apk/release/app-release.apk ./lifeos.apk

The download links on the site point at `/lifeos.apk`.
